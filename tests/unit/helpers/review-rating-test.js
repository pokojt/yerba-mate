import { reviewRating } from 'yerba-mate/helpers/review-rating';
import { module, test } from 'qunit';

module('Unit | Helper | review rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reviewRating([42]);
  assert.ok(result);
});
